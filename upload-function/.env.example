# Required for local development.
GCS_BUCKET=
PUBLIC_BASE_URL=https://artworks.artnet.com

# Optional. Defaults to 900 seconds.
SIGNED_URL_TTL_SECONDS=900

# Optional. Defaults to * for local development.
CORS_ALLOW_ORIGIN=*

# Required. Cloudflare R2 replication target (spec §2.9).
R2_ACCOUNT_ID=<your-r2-account-id>
R2_BUCKET=<your-r2-bucket>
R2_ACCESS_KEY_ID=<your-r2-access-key-id>
R2_SECRET_ACCESS_KEY=<your-r2-secret-access-key>

# Optional. Replication PUT/DELETE retry count (additional attempts after the first). Defaults to 3.
R2_REPLICATION_RETRIES=3
